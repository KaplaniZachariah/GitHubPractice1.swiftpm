import SwiftUI

struct ContentView: View {
    @State var Answer = 0
    var body: some View {
        VStack{
            Button(<#T##title: StringProtocol##StringProtocol#>, action: <#T##() -> Void#>)
            
            Text(Answer)
        }
    }
}
